<div class="room-info">
  <div class="heading">
    <%=link_to room.heading, room_path(room.id)%>
  </div>
  <p>部屋の1コメここに内容が入るここに内容が入るここに内容が入るここに内容が入るここに内容が入るここに内容が入るここに内容が入るここに内容が入る</p>
  <div class="category">
  <span>カテゴリー:</span>
  <%=room.category.name%>
  </div>
  <div class="tags">
  <span>タグ:</span>
    <%room.tags.each do |tag|%>
      <li class="tag"><%=tag.name%></li>
    <%end%>
  </div>
</div>